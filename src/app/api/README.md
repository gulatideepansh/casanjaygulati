# API Planning

No API routes are required for version 1 of the public website.

This folder is reserved for future backend handlers such as:
- Authentication callbacks
- Employee login/session APIs
- Document metadata and secure download endpoints
- Attendance or task module actions
- Admin content update endpoints

Only add custom API routes when a real business workflow requires them.
